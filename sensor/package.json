{
  "name": "homesensor-node",
  "version": "0.0.1",
  "description": "Runs on sensor node and sends events to server.",
  "main": "index.js",
  "scripts": {
    "test": "node index.js",
	"start": "pm2 start index.js --watch",
	"stop": "pm2 stop --watch 0"
  },
  "author": "Justin Jordan",
  "license": "ISC",
  "dependencies": {
    "node-lifx": "^0.8.0",
    "onoff": "^1.1.8",
    "rpi-gpio": "^0.9.1"
  }
}
